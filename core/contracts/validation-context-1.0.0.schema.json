{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Explicit research-os validate --context input. All refs are resolved at their declared fixed locator and SHA-256; no path/version inference or mutable-worktree fallback. Records may contain typed Artifacts or opaque original bytes.",
  "type": "object",
  "required": ["project", "evidence"],
  "additionalProperties": false,
  "properties": {
    "project": {"$ref": "publication/definitions-1.0.0.schema.json#/$defs/ref"},
    "evidence": {"$ref": "publication/definitions-1.0.0.schema.json#/$defs/refs"},
    "records": {"$ref": "publication/definitions-1.0.0.schema.json#/$defs/refs"}
  }
}
